# OPTIONAL — the server runs fully WITHOUT this.
# RxNorm/RxClass need no key. openFDA works keyless (~240 req/min, ~1000 req/day per IP).
# A free openFDA API key only raises the daily ceiling — useful for heavy use.
# Get one at https://open.fda.gov/apis/authentication/
OPENFDA_API_KEY=
